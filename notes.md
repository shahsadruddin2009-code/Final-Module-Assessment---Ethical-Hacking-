# Notes

## Branching
- Use `git switch -c <branch-name>` to create and switch to a new branch.
- Use `git switch <branch-name>` to switch between branches.
- Branches allow you to work on features or fixes without affecting the main codebase.
